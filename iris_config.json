{
  # The graph for karma to generate.
  "graph_1" :
  {
    # The modules as a part of the graph.
    "Modules" : 
    {
      # Vulkan API Module.
      "API" :
      {
        "type"  : "Vulkan",
	"input" : "window_surface"
      },
      # Vulkan Window Module.
      "window" :
      {
	"type"   : "Vulkan Window",
        "output" : "window_surface",
        "width"  : 1024,
        "height" : 720
      }
    }
  }
}
